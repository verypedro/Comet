banner.png and banner.wav are now part of the project -- nothing to
supply here for the CIA build.

app.rsf is the CIA's permissions/metadata file and doesn't need
editing unless you want to change the title ID (see the comment next
to UniqueId inside it) or add a permission a future feature needs.
